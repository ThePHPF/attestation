includes:
    - vendor/phpstan/phpstan-webmozart-assert/extension.neon

parameters:
    level: 10
    paths:
        - src
        - test/unit
        - test/integration
    # Exclude vendored flow-php/snappy files
    excludePaths:
        - src/FlowSnappy/*
